<div class="admin-dashboard-container">
  <h1>Bem-vindo ao Painel Administrativo</h1>
  <p>Gerencie o catálogo de rodas e outras informações da aplicação.</p>
  <div>
  </div>
  <style>
    /* Painel Administrativo */
    .admin-dashboard-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px;
    background-color: var(--light);
    color: var(--dark);
    border: 1px solid var(--secondary);
    border-radius: 10px;
    max-width: 800px;
    margin: 20px auto;
    }
    .admin-dashboard-container h1 {
    color: var(--primary);
    text-align: center;
    margin-bottom: 20px;
    }
    .admin-dashboard-container p {
    color: var(--secondary);
    font-size: 16px;
    margin-bottom: 30px;
    text-align: center;
    }
    .admin-dashboard-container .btn {
    margin: 10px;
    padding: 10px 20px;
    font-size: 16px;
    font-weight: bold;
    }
    .admin-dashboard-container .btn-primary {
    background-color: var(--primary);
    color: #fff;
    }
    .admin-dashboard-container .btn-primary:hover {
    background-color: #b90f1c;
    }
  </style>
